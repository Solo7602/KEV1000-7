using System;

class Graph
{
    private int V; // количество вершин
    private int[,] adjMatrix; // матрица смежности

    public Graph(int v)
    {
        V = v;
        adjMatrix = new int[V, V];
    }

    public void AddEdge(int v, int w)
    {
        adjMatrix[v, w] = 1;
        adjMatrix[w, v] = 1; // если граф ненаправленный
    }

    public void PrintGraph()
    {
        for (int i = 0; i < V; i++)
        {
            for (int j = 0; j < V; j++)
            {
                Console.Write(adjMatrix[i, j] + " ");
            }
            Console.WriteLine();
        }
    }
}

class Program
{
    static void Main()
    {
        Graph g = new Graph(4);

        g.AddEdge(0, 1);
        g.AddEdge(0, 2);
        g.AddEdge(1, 2);
        g.AddEdge(2, 3);

        g.PrintGraph();
    }
}
Объяснение:
У нас строиться матрица, где мы проставляем если есть дуга между вершинами, то ставим 1, если между ними дуги нет, то 0
