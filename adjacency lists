using System;
using System.Collections.Generic;

class Graph
{
    private int V; // количество вершин
    private List<int>[] adj; // списки смежности

    public Graph(int v)
    {
        V = v;
        adj = new List<int>[V];
        for (int i = 0; i < V; i++)
        {
            adj[i] = new List<int>();
        }
    }

    public void AddEdge(int v, int w)
    {
        adj[v].Add(w);
        adj[w].Add(v); // если граф ненаправленный
    }

    public void PrintGraph()
    {
        for (int i = 0; i < V; i++)
        {
            Console.Write("Вершина " + i + " смежна с: ");
            foreach (var vertex in adj[i])
            {
                Console.Write(vertex + " ");
            }
            Console.WriteLine();
        }
    }
}

class Program
{
    static void Main()
    {
        Graph g = new Graph(4);

        g.AddEdge(0, 1);
        g.AddEdge(0, 2);
        g.AddEdge(1, 2);
        g.AddEdge(2, 3);

        g.PrintGraph();
    }
}
Объяснение:
У каждой вершины у нас есть список вершин, с которыми она смежна.
